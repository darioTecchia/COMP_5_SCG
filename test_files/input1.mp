global
    result: float = 1.0;
    buffer: float;
    x: int
end

function pluto(y: float): string
    y = y - 1;
    return y
end

function main(): nil
     buffer <==;
     pluto(buffer);
     result = addInc(result + 1, buffer);
     "la somma risulta: ", result ==>
end

function addInc(i: float): float
    x = x + 1;
    local
        str: string = 2.0;
        i = i + 1;
        local
            pippo: float = 1.0;
            pippo = pippo + 1
         end;
         return x
    end
end